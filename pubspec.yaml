name: pexels_photos_videos
description: This package will help you to get data from pexels.com. It's easy to use and get all details related to media which is requested.
version: 0.0.15

# Add the homepage field
homepage: https://tarunmahajan.com

# Add the repository field
repository: https://github.com/tango4567/pexels_photos_videos

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
flutter:
